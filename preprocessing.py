from spectrogram import process_dataset
import numpy as np

# Dummy example (replace with real dataset later)
signals = [np.random.randn(22050) for _ in range(10)]

# Generate spectrograms
process_dataset(signals)


        return self.fc(final_out)
